package delta.dkt.logic.structure;

import android.util.Log;

public class GameMap {

    public Field getField (int location) {
        Log.d("Todo", "I am using the location parameter, which will be used when this method is implemented, to get rid of the code smell." + location);
        return null;
    }
}
